# Create Component

Create a new reusable component: $ARGUMENTS

1. Create PHP template part in `template-parts/`
2. Create corresponding SCSS partial in `assets/scss/components/`
3. Import the SCSS partial in `main.scss`
4. Add any necessary JavaScript to `assets/js/`
5. Document usage in the component file header
