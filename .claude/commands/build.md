# Build Assets

Compile SCSS and prepare assets for production.

1. Run `npm run build` to compile SCSS to CSS
2. Verify the compiled CSS in `assets/css/main.css`
3. Check for any compilation errors
4. Report the build status
